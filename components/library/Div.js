import styled from '@emotion/styled'
import { space, fontSize, width, color } from 'styled-system'

const Div = styled('div')([], props => ({}), space, fontSize, width, color)

Div.defaultProps = {}

export default Div
