import styled from 'styled-components'
import { space, fontSize, width, color } from 'styled-system'

const Form = styled('form')([], props => ({}), space, fontSize, width, color)

Form.defaultProps = {}

export default Form
