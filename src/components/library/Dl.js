import styled from '@emotion/styled'
import { space, fontSize, width, color } from 'styled-system'

const Dl = styled('dl')([], props => ({}), space, fontSize, width, color)

Dl.defaultProps = {}

export default Dl
